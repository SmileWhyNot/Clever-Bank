## Необходимо создать файл application.yml со структурой, приведенной ниже
db:
  url: ## here your DB url like jdbc:postgresql://localhost:5432/Example
  user: ## here your DB username, default=postgres
  password: ## here your DB password, default=postgres